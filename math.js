function add(a, b) {
    return a + b;
} 

function subtract(a, b) {
    return a - b;
}

// module.exports = add;
module.exports = {
    add,
    subtract,
}

// module.exports = {
//     addFunction: add,
//     subtractFunction: subtract,
// };

